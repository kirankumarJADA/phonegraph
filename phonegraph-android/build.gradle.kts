// PhoneGraph Android Client — top-level build file.
// Minimal demonstration client per the dissertation proposal: single
// screen, TextField, Ask button, calls the backend, displays the
// recommendation. See app/build.gradle.kts for dependencies.

plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
